#include <stdio.h>
//implementation of array in c

const TOTAL = 3;

int main(void)
{
    int scores[TOTAL];
}

float average(int length, int array[])
{
    int sum = 0;
    for (int i = 0; i < length, i++)
    {
        sum = sum + array[]
    }
}